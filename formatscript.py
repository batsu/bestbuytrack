
def formatjson(data):

	formatstr = []

	#for i, x in enumerate(data["items"]):
	"""
		holder = {}
		holder["title"] = x["title"]
		holder["sku"] = x["sku"]
		holder["options"] = []
		for y in x["prices"]:
			holder2 = {}
			holder2["condition"] = y["condition"]
			holder2["price"] = str(y["prices"]["current"])
			if y["onlineAvailability"] | y["inStoreAvailability"]:
				holder2["link"] = x["web"]
			holder["options"].append(holder2)
		formatstr.append(holder)
	"""
			

	return data